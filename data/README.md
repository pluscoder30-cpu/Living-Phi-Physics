# THE DATA — odlyzko_zeros1.txt (100,000 zeta zeros) and related

**What this is:** the corpus data store at the ledger root.

| File | What it is |
|---|---|
| `odlyzko_zeros1.txt` | **100,000 zeta-function zeros** (1,800,000 bytes; first three: 14.134725142, 21.022039639, 25.010857580) — the canonical shared input, the Riemann-register data source. The canonical Odlyzko set for the corpus's 2026-08-14 Riemann computation. |

**Companion data:** the full 2026-08-14 real-data campaign inputs live in `../verification/data/` — `odlyzko_zeros1–6.txt` (all six Odlyzko tranches), the JPL inputs (`jpl_horizons_elements.json`, `jpl_planetary_facts.json`), the exoplanet input (`exoplanet_multi_planets.json`), and the fetch scripts that obtained them (`fetch_odlyzko_all.py`, `fetch_horizons.py`, `fetch_jpl_planetary.py`, `fetch_exoplanets.py`).

**Where it is used:** the Riemann φ-gap computation (predictions in `../00_UNIFIED_FIELD_THEORY.md` §15) — the confirmed result is the verified ledger [`../verification/CONFIRMED_RESULTS.md`](../verification/CONFIRMED_RESULTS.md).

---

*Register of `32_PHI_PHYSICS/` — the data store of the ledger. See [`../INDEX.md`](../INDEX.md).*